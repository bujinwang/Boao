// React Native Expo TypeScript .cursorrules

// Best practices
const bestPractices = [
  "Utilize Expo SDK features and APIs",
  "Implement proper navigation using React Navigation",
  "Use Expo's asset system for images and fonts",
  "Implement proper error handling and crash reporting",
];

// Folder structure
const folderStructure = `
assets/
src/
  components/
  screens/
  navigation/
  hooks/
  utils/
  services/
    apis/
    constants/
    types/
App.tsx
app.json
`;

// Coding guidelines
const codingGuidelines = `
1. Use TypeScript for type safety
2. Implement proper styling using StyleSheet or styled-components
3. Utilize Expo's vector icons
4. Use Expo's secure store for sensitive data
5. Implement proper offline support
6. Follow React Native best practices for performance
7. Use Expo's OTA updates for quick deployments
8. Use camelCase for variables and functions, PascalCase for components
9. Prefer named exports for components
10. Use interfaces for props and state
11. Avoid using 'any' type; strive for precise types
12. Use React.FC for functional components with props
13. Implement responsive design using Flexbox and Expo's useWindowDimensions
14. Use SafeAreaView for proper screen inset handling
15. Optimize performance with useMemo and useCallback hooks
16. Use react-query for data fetching and caching
17. Implement proper error boundaries and logging
18. Write unit tests using Jest and React Native Testing Library
19. Use Expo's localization tools for internationalization
`;

// Component structure
const componentStructure = `
components/
  MyComponent/
    index.tsx
    styles.ts
    types.ts
`;

// Example component
const exampleComponent = `
import React from 'react';
import { View, Text } from 'react-native';
import { MyComponentProps } from './types';
import { styles } from './styles';

const MyComponent: React.FC<MyComponentProps> = ({ title }) => {
  return (
    <View style={styles.container}>
      <Text style={styles.title}>{title}</Text>
    </View>
  );
};

export default MyComponent;
`;

// You are an expert in TypeScript, React Native, Expo, and Mobile App Development
